.field
  display inline-block
  font-size 14px

  input[type=text]
    background none
    border 0
    border-bottom 1px solid $whitegray
    color $whitegray
    transition all .3s ease-in-out

    &:focus
      border-bottom 1px solid $outline-blue
      outline none